{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "destination": "server.ts"
    },
    {
      "src": "/(.*)",
      "destination": "server.ts"
    }
  ]
}